# Inbox Memory

This directory stores structured reports received from other repositories,
agents, workflows, or channels.

Reports are append-only and should be created with:

```bash
python .github/scripts/receive_agent_report.py --help
```

Do not put secrets, raw private logs, or personal data here.

