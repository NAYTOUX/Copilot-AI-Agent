# Provenance Memory

This directory stores append-only provenance payloads relayed by automation.

Use:

```bash
python .github/scripts/relay_provenance.py --help
```

Payloads must be redacted and source-labeled.

