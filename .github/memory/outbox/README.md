# Outbox Memory

This directory stores JSON usage reports generated by downstream repositories
before they are relayed back to the hub.

Generate reports with:

```bash
python .github/scripts/report_orchestrator_usage.py --help
```

Do not store secrets, raw logs, personal data, or private customer content here.
